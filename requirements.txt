flask==3.0.3
waitress==3.0.2
requests==2.32.3
pdfplumber==0.11.4
plotly==6.9.0
pandas==2.3.3
spacy==3.7.5
scispacy==0.5.4
# The scispaCy model isn't on PyPI proper — pip can still install it directly
# from this URL. It's ~150MB, so this step takes a while on first install.
en_core_sci_sm @ https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.4/en_core_sci_sm-0.5.4.tar.gz
